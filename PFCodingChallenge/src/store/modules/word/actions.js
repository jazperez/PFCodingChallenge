import Word from '../../api/Word';

export default{

    fetchWordFromFile1({commit, dispatch}){
        Word.fetchWordFromFile1(
            (success) => {
                
            }
        )
    }
}